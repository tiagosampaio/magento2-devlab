version: "2"
syncs:
  #IMPORTANT: ensure this name is unique and does not match your other application container name
  magento2-sync: #tip: add -sync and you keep consistent names as a convention
    src: './sites/magento2'
options:
  compose-file-path: './app/compose/docker-compose.yml'
  compose-dev-file-path: './app/compose/docker-compose.mac.yml'
  verbose: false
